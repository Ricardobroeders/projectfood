# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Copy: every user-facing string (i18n files, notification and celebration text) goes through
the `pf-voice` skill at the repo root; the brand files are in `knowledge-base-general/wiki/brand/`.
